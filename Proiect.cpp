#include <iostream>
#include <string>
#include "database.h"
#include <iomanip>

using namespace std;

int main(int argc, char** argv)
{
	/*string* s = new string[argc];
	for (int i = 0; i < argc; i++)
	{
		s[i] = argv[i];
	}
	s[0] = "cfg.txt";
	database a;
	consola c;
	c.crud(a); */
	readfile cfg;
	return 0; 
	
}
